# Files the agent must not read or edit at all. Everything that only needs to stay
# out of the semantic index lives in .cursorindexingignore instead, so it remains
# readable on demand.

# Large third-party submodule — never edited from this repo (see AGENTS.md → Do not)
externals/vcpkg/
