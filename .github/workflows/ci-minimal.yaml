name: CI Minimal (Linux Debug only)

on:
  pull_request:

jobs:
  win-debug:
    uses: ./.github/workflows/ci-steps.yaml
    with:
      os: windows-2022
      build_type: debug
    secrets:
      GH_PACKAGES_TOKEN: ${{ secrets.GH_PACKAGES_TOKEN }}